<UserControl x:Class="Fruitylator.Views.RawEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:interfaces="clr-namespace:Fruitylator.Core.Interfaces;assembly=Fruitylator.Core"
             xmlns:viewModels="clr-namespace:Fruitylator.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             d:DataContext="{d:DesignInstance viewModels:RawEditorViewModel}">
    <Grid>
        <ContentControl x:Name="Part">
            <ContentControl.ContentTemplate>
                <DataTemplate DataType="interfaces:ITranslatablePart">
                    <TextBox Text="{Binding Text, Mode=TwoWay}"
                             TextWrapping="Wrap"
                             Style="{StaticResource EditorTextBoxStyle}" />
                </DataTemplate>
            </ContentControl.ContentTemplate>
        </ContentControl>
    </Grid>
</UserControl>



